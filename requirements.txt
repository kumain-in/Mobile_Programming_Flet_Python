# Manajemen Mahasiswa — runtime deps.
# Local desktop/browser dev needs only flet + openpyxl; the rest are for
# production web serving (uvicorn ASGI) used by the Docker/Dokploy deploy.
flet==0.85.2
flet-web==0.85.2
openpyxl>=3.1,<4.0
uvicorn==0.49.0
